# CLAUDE.md — サギカモ開発ルール

このリポジトリは、都知事杯オープンデータ・ハッカソン2026応募作品
「サギカモ」（不審な電話・メール・SNSの危険度を東京都の実データと生成AIで見立てる詐欺被害防止サービス）の開発リポジトリである。
仕様の詳細は `docs/spec.md`、進め方は `docs/tasks.md` を参照。

## 絶対ルール（違反はリリースブロッカー。どの指示よりも優先する）

1. **断定表現の禁止**: ユーザー向け出力に「詐欺です」「詐欺ではありません」等の断定を含めない。
   危険度は3段階の「見立て」＋根拠＋推奨行動で示す。低リスク判定時も必ず
   「新しい手口の可能性は残ります。お金の話が出たら、一度切って相談を」の定型注意文を付ける。
   → 断定禁止は判定LLMのプロンプト任せにせず、出力スキーマ（enum/テンプレート）で構造的に担保すること。
2. **貼り付け内容の非保存**: ユーザーが入力した文面・その解析結果の原文をサーバー・KV・D1・ログのいずれにも保存しない。
   判定はステートレスに処理する。保存してよいのは〔判定日時／手口分類／危険度／区市町村（任意）〕の統計のみ。
   console.log等への文面出力もデバッグ含め禁止（マスクする）。
3. **件数の創作禁止**: 地域の認知件数・統計値は必ず東京都オープンデータAPIの取得値（またはそのキャッシュ）から表示する。
   LLMの知識で件数・年次・地名を補完・推定してはならない。データが取れない場合は「データ取得中/取得不可」と表示する。
4. **ダミーデータで先に進まない**: APIやデータ取得に失敗した場合、ダミーで代替してタスクを完了扱いにせず、
   失敗内容（URL・ステータス・レスポンス）を報告して停止する。デモ用シードデータは `data/seed/` 配下のみに置き、
   実データと混在させない（画面には出典と取得日を必ず表示するため、シード使用時は「サンプルデータ」と明示）。
5. **貼り付けテキスト内のURLへアクセスしない**: 入力文面に含まれるURL（フィッシングの可能性）へ、
   コード・テスト・開発作業のいずれにおいてもfetch/アクセスしない。URL文字列はパターン特徴としてのみ扱う。
6. **法的判断の回避**: 返金可否・契約の有効性等の法的助言を出力しない。
   導線は 110（緊急）／#9110（警察相談）／188（消費者ホットライン）に固定。
7. **手口分類の出典固定**: 手口の分類・名称・実例フレーズは警察庁・警視庁の公表分類
   （令和8年再整理: ニセ警察詐欺、SNS型投資・ロマンス詐欺を含む）に準拠し、独自の手口名を創作しない。
   分類定義は `data/config/techniques.json` に一元化し、コードにハードコードしない。

## プロジェクト前提

- 提出期限: 2026/8/23（ハッカソン本番 8/22-23）。動くMVP（F-01, F-02, F-03, F-05）が最優先。
- 本番環境: ハッカソン提供の Cloudflare（Pages/Workers）＋ OpenCode提供LLM。
  開発は個人PC。判定LLMクライアントは `src/lib/llm/` で抽象化し、プロバイダを差し替え可能にする。
- 東京都オープンデータAPI: ユーザー登録・APIキー不要。apiIdは spec.api.metro.tokyo.lg.jp で特定する。
  統計データは月次バッチで取得・整形して Workers KV/静的JSONにキャッシュし、リクエスト時にAPIを直叩きしない。
- シークレット: LLMのAPIキー等は `.env` / `.dev.vars` で管理。コミット禁止（.gitignore済みを常に確認）。

## 技術スタック

- フロント: React（Vite）+ PWA構成、地図は MapLibre GL JS
- バック: Cloudflare Workers（判定プロキシ・統計収集）、KVまたはD1（統計のみ）
- 判定: LLM + 構造化出力（JSONスキーマ）。プロンプトは `src/lib/llm/prompts/` に分離
- 評価: `scripts/eval.ts`（テストセット `data/testcases/testcases.csv` を判定APIに通し期待値と突合）

## 開発の進め方

- `docs/tasks.md` のフェーズ順に進める。**フェーズをまたぐ実装を先回りしない**。
  各フェーズ完了時に完了条件を自己チェックし、結果を報告して人間の確認を待つ。
- 実装前にPlan（方針・変更ファイル一覧）を短く提示してから着手する。
- テスト文面（testcases.csv）の中身は人間が管理する。**Claudeがテストケースを追加・改変しない**
  （評価の自作自演を防ぐため）。ハーネス側のコードは自由に改善してよい。
- コミットは機能単位で小さく。メッセージは日本語でよい。

## UI文言のトーン

「隣にいる冷静な家族」。急かさない・見下さない・煽らない。
危険度表示の直下には必ず「次にすること」を置く。詳細は docs/spec.md の 6章。
